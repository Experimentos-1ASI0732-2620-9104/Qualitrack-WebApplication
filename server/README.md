# Legacy test fixtures

These JSON Server files are retained as historical, explicitly local fixtures.
The real application does not use them. Do not start this server for production
or use its accounts, plans, subscriptions or measurements as operational data.
Use the Spring Boot API. Automated test fixtures belong to the test harness.
